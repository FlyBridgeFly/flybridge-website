---
import SubjectPage from "../components/SubjectPage.astro";
import { subjectPages } from "../data/site";

const subject = subjectPages.find((item) => item.slug === "gcse-chemistry");
if (!subject) {
  throw new Error("GCSE Chemistry subject data not found.");
}
---

<SubjectPage subject={subject} />
