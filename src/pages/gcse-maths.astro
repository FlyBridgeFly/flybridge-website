---
import ExpansionSubjectPage from "../components/ExpansionSubjectPage.astro";
import { expansionSubjectPages } from "../data/site";

const subject = expansionSubjectPages.find((item) => item.slug === "gcse-maths");

if (!subject) {
  throw new Error("GCSE Maths page data not found.");
}
---

<ExpansionSubjectPage subject={subject} />
