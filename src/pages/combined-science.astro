---
import SubjectPage from "../components/SubjectPage.astro";
import { subjectPages } from "../data/site";

const subject = subjectPages.find((item) => item.slug === "combined-science");
if (!subject) {
  throw new Error("Combined Science subject data not found.");
}
---

<SubjectPage subject={subject} />
