---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;
---

<article class="card-panel p-8 text-center">
  <h2 class="text-2xl font-semibold tracking-tight text-fb-ink">{title}</h2>
  <p class="mt-3 text-base leading-7 text-fb-ink-soft">{body}</p>
</article>
